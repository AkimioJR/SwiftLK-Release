## v1.0.0 Build 6

### 更新日志

#### 🚀 新功能
- **评论系统**：支持按回复 ID 排序，优化评论渲染效果；新增回复用户链接与文本高亮显示；完善回复按钮的交互逻辑；新增评论加载与刷新功能，简化评论加载流程。
- **关注动态**：支持动态加载关注的文章；优化关注文章卡片的视觉效果，调整封面图圆角；新增分享链接与用户关注状态显示。
- **文章功能**：新增文章排序功能，支持按顺序/更新时间升降序排列；支持切换正序/倒序显示；优化排序方式与方向的选择逻辑；新增查找文章及获取最后阅读文章功能，支持上次阅读标记与快速跳转；为文章行视图添加过渡动画，展示文章封面与信息；将部分视图替换为懒加载模式以优化性能；简化文章内容的 BBCode 处理逻辑，新增加载状态。
- **用户界面**：为登录按钮添加填充与背景样式；优化未登录状态下用户信息区域的显示逻辑；优化个人页面背景样式；启用评论与评分的文本选择功能；更新奖牌展示区域，直接使用用户奖牌数据；新增用户头像禁用逻辑（用户 ID 为 0 时禁用）；在关于页面添加构建类型信息，区分 DEBUG/RELEASE 版本；支持跳转集合。
- **任务系统**：新增用户任务管理功能，简化任务加载与按钮逻辑。

#### 🔧 优化
- 重构分享与签到日期逻辑，统一数据管理；简化应用初始化逻辑；优化部分视图模型，移除不必要的刷新状态，支持首次加载状态；提取上次阅读跳转条为独立函数，简化代码结构。

#### 🐛 修复
- 修复分享任务状态错误问题；
- 修复分享按钮模拟触发函数无法正确触发问题；
- 修复个人页面拼写错误，确保注销警报正确显示；
- 修复某些文章仅勇者可看的提示。

#### 🔄 重构
- 重命名部分视图（如主题详情视图、任务视图）；
- 重构用户任务视图，使用视图模型管理状态；
- 重构记录视图方法，支持自定义 ID，简化数据处理逻辑；
- 将日期判断属性标记为非隔离状态。
